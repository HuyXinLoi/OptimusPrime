import 'package:flutter/material.dart';

class ProductdetailScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Productdetail Screen'),
    );
  }
}
